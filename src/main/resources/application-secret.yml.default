spring:
  security:
    oauth2:
      client:
        registration:
          kakao:
            clientId: 인증받은키값
custom:
  security:
    oauth2:
      client:
        registration:
          kakao:
            devUser:
              oauthId: oauthId
              nickname: nickname
              profileImgUrl: profileImgUrl